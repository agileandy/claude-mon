# macOS
.DS_Store
*.swp
*.swo

# Swift Package Manager
.build/
.swiftpm/
Packages/
# Package.resolved is intentionally committed — apps want reproducible deps.

# Xcode (in case someone generates a project later)
*.xcodeproj/
*.xcworkspace/
DerivedData/
build/
*.app/
xcuserdata/
*.xcuserstate

# Local Claude Code settings (user-specific overrides)
.claude/settings.local.json

# Internal planning + retro notes — keep local, don't publish
.claude/plans/
data/

# Logs & scratch
*.log
*.tmp
